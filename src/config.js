export const BATCH_SIZE = 20
export const MIN_WORDS_FOUND = 0
export const CUT_FREQUENCY = 1 // One out of every n pages will on average be cut
export const WORD_FREQUENCY = 3 // One of of every n words will on average be cut
export const SCHEDULER_WORKERS = 3
export const DEFAULT_IMAGE_MAX_WIDTH = 600
